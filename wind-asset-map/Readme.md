# Interactive Wind Asset Map 🇩🇰

A Python tool that generates an interactive HTML map of wind turbines, parks, and owner information in Denmark.

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Run the script: `python generate_map.py`